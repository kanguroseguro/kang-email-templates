{
  "name": "kang-email-templates",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && mjml src/*.mjml -o dist/",
    "watch": "mjml src/*.mjml -o dist/ --watch",
    "test-loader": "node template-loader.js"
  },
  "dependencies": {
    "mjml": "^4.15.3"
  }
}
